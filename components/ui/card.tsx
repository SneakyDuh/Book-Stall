import * as React from 'react'; export function Card(p){return <div className={`bg-white p-5 rounded-2xl shadow ${p.className||''}`}>{p.children}</div>} export function CardHeader(p){return <div className={`mb-3 ${p.className||''}`}>{p.children}</div>} export function CardTitle(p){return <h3 className={`font-semibold text-lg ${p.className||''}`}>{p.children}</h3>} export function CardContent(p){return <div className={`text-sm ${p.className||''}`}>{p.children}</div>}